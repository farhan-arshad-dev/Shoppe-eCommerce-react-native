import { StyleSheet } from "react-native";

export default StyleSheet.create({
    categoryContainer: {
        width: "100%",
        marginTop: 20,
    },
    listHeader: {
        width: "100%",
        flexDirection: "row",
        alignItems: "center",
        marginBottom: 12,
        justifyContent: "space-between"
    },
    listTitle: {
        fontSize: 21,
        fontWeight: "bold",
        lineHeight: 30,
        color: "#202020",
        marginRight: 8,
    },
});
