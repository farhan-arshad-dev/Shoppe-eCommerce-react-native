import { StyleSheet } from "react-native";

export default StyleSheet.create({
    secondaryButton: {
        width: "100%",
        backgroundColor: "#FF5790",
        borderRadius: 16,
    },
    secondaryButtonText: {
        color: "#F3F3F3",
        margin: 16,
        fontSize: 22,
        fontWeight: "300",
        textAlign: "center",
    },
});
