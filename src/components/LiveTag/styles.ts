import { StyleSheet } from "react-native";

export default StyleSheet.create({
    liveTextContainer: {
        backgroundColor: "#08C514",
        borderRadius: 7,
    },
    liveText: {
        color: "#fff",
        fontSize: 12,
        fontWeight: "700",
        paddingHorizontal: 8,
        paddingVertical: 4,
    },
});
