import { StyleSheet } from "react-native";

export default StyleSheet.create({
    seeAllContainer: {
        height: "100%",
        flexDirection: "row",
        alignItems: "center",
    },
    seeAllText: {
        fontSize: 15,
        fontWeight: "bold",
        color: "#202020",
        marginEnd: 16,
    },
});
